# iris
This project uses measurements of Iris flowers (setosa, versicolor, and virginica) to train a machine learning model that classifies the species based on their features. Using Scikit-learn, the dataset is accessed, a classification model is built, and its accuracy and performance are evaluated using test data.
